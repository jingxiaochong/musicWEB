name: music_web

services:
  musicWEB:
    build:
      context: .
    container_name: musicWEB
    restart: unless-stopped
    ports:
      - "${NCM_WEB_BIND_ADDRESS:-127.0.0.1}:${NCM_WEB_PORT:-8080}:80"
    networks:
      - ncm_net

networks:
  ncm_net:
    external: true
    name: ncm_net
